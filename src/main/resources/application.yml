spring:
  security:
    user:
      name: buzz
      password: infinity
  datasource:
    generate-unique-name: false
    name: tacocloud
greeting:
  welcome: Welcome, ${spring.application.name}!

taco:
  orders:
    pageSize: 10


  discount:
    codes:

      abcdef: 10
logging:
  level:
    root: WARN
    org:
      springframework:
        security: DEBUG
  file:
    path: /var/logs/
    name: TacoCloud.log

---
spring:
  datasource:
    url: jdbc:mysql://localhost/tacocloud
    username: tacouser
    password: tacopassword
  config:
    activate:
      on-profile: prod

logging:
  level:
    tacos: WARN
